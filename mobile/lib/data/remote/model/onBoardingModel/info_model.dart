class InfoModel {
  String imagePath, title, subtitle, description;

  InfoModel(
      {required this.title,
      required this.imagePath,
      required this.subtitle,
      required this.description});
}
